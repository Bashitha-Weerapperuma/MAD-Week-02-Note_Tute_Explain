// Creating an array of module names
fun main() {
    val moduleNames = arrayOf(
        "MAD",
        "DSA",
        "ITP",
        "PS",
        "ESD",
        "PAS"
    )

    // Printing the module names
    println("Modules you'll learn this semester:")
    for (moduleName in moduleNames) {
        println(moduleName)
    }
}
